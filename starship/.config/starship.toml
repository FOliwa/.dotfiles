# onedark vibe (przybliżone kolory)
[palettes.onedark]
fg = "#abb2bf"
bg = "#1e222a"
blue = "#61afef"
green = "#98c379"
purple = "#c678dd"
red = "#e06c75"
yellow = "#e5c07b"
cyan = "#56b6c2"

# używamy tej palety
palette = "onedark"

# ogólny format: katalog, git, python, znak zachęty
format = """
($directory)
($git_branch$git_status)($python)
$character
"""

[directory]
truncate_to_repo = false
truncation_length = 0

[git_branch]
symbol = '🌱 '
truncation_length = 10
truncation_symbol = '…'
ignore_branches = []

[git_commit]
commit_hash_length = 6
tag_symbol = '🔖 '

[git_status]
conflicted = '🏳'
ahead = '🏎💨'
behind = '😰'
diverged = '😵'
up_to_date = '✓'
untracked = '🤷'
stashed = '📦'
modified = '📝'
staged = '[++\($count\)](green)'
renamed = '👅'
deleted = '🗑'

[python]
pyenv_version_name = true
format = 'via [${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'

[character]
success_symbol = "[➜](fg:green) "
error_symbol = "[➜](fg:red) "

# wywalanie śmieci, których nie chcesz
[hostname]
disabled = true
[time]
disabled = true
[username]
disabled = false
